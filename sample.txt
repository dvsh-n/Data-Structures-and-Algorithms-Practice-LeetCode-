def maximumBags(capacity, rocks, additionalRocks):
        result = 0
        diff = []
        max = 0
        for i in range(len(capacity)):
            diff_i = capacity[i] - rocks[i]
            if diff_i > max:
                max = diff_i
            diff.append(diff_i)

        data = [0 for j in range(max)]
        print(data)
        print(diff)

        for i in diff:
            data[i] += i
        print(data)

        # for i in range(len(capacity)):
        #     diff.append(capacity[i] - rocks[i])
        #     if diff[i] == 0:
        #         result += 1
        # for j in range(1,len(additionalRocks)):
            #     if j < additionalRocks:
            #         additionalRocks -= j
            #         result += 1
            #     else:
            #         if additionalRocks != 0:
            #             additionalRocks = 0
            #             result += 1
        return result